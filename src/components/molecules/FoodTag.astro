---
import FoodIcon from "../atoms/icons/FoodIcon.astro";

const {
  icon: Icon = null,
  color = "#fff",
  ariaLabel = "",
  class: className = "food-tag",

  ...rest
} = Astro.props;
---

<div aria-label={ariaLabel} class={className}>
  {
    Icon && (
      <FoodIcon icon={Icon}   />
    )
  }
</div>

<style>
  /* .food-tag {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--secondary-500);
    border: 3px solid var(--primary-500); */
  
    border-radius: 9999px;
  }

  .food-tag svg {
    width: 25px;

    display: block;
    fill: #fff;
  }
</style>
